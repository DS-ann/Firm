name: Build ESP32 Firmware

on:
  push:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Force Node.js 24

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # -------------------------
      # Checkout repository
      # -------------------------
      - name: Checkout repository
        uses: actions/checkout@v5

      # -------------------------
      # Install Arduino CLI
      # -------------------------
      - name: Install Arduino CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          sudo mv bin/arduino-cli /usr/local/bin/

      # -------------------------
      # Setup Arduino CLI
      # -------------------------
      - name: Setup Arduino CLI
        run: |
          arduino-cli config init
          arduino-cli core update-index
          arduino-cli core install esp32:esp32

      # -------------------------
      # Install libraries
      # -------------------------
      - name: Install required libraries
        run: |
          # Indexed libraries
          for lib in "ArduinoJson" "PubSubClient" "Preferences"; do
            arduino-cli lib install "$lib"
          done

          # Download real NimBLE‑Arduino release zip
          mkdir -p /tmp/nimble
          curl -L -o /tmp/nimble/NimBLE_Arduino.zip \
            https://arduinolibraries.info/download/nim-ble-arduino/NimBLE_Arduino-2.3.7.zip

          # Show file info (for debugging)
          ls -lh /tmp/nimble/
          file /tmp/nimble/NimBLE_Arduino.zip

          # Unzip and install locally
          unzip -o /tmp/nimble/NimBLE_Arduino.zip -d /tmp/nimble/
          arduino-cli lib install /tmp/nimble/NimBLE_Arduino

      # -------------------------
      # Compile firmware
      # -------------------------
      - name: Compile firmware
        run: |
          arduino-cli compile \
            --fqbn esp32:esp32:esp32 \
            --build-property partition_scheme=huge_app \
            --output-dir build \
            .

      # -------------------------
      # Prepare flash files
      # -------------------------
      - name: Prepare flash files
        run: |
          mkdir -p firmware
          cp build/*.bin firmware/

      # -------------------------
      # Upload firmware artifact
      # -------------------------
      - name: Upload firmware
        uses: actions/upload-artifact@v5
        with:
          name: esp32-firmware
          path: firmware
